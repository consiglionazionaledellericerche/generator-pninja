<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up()
    {
        // Controlla e rinomina solo se le tabelle esistono
        if (Schema::hasTable('jobs')) {
            Schema::rename('jobs', 'queue__jobs');
        }
        
        if (Schema::hasTable('job_batches')) {
            Schema::rename('job_batches', 'queue__job_batches');
        }
        
        if (Schema::hasTable('failed_jobs')) {
            Schema::rename('failed_jobs', 'queue__failed_jobs');
        }
    }

    /**
     * Reverse the migrations.
     *
     * @return void
     */
    public function down()
    {
        // Controlla e rinomina solo se le tabelle con prefisso esistono
        if (Schema::hasTable('queue__failed_jobs')) {
            Schema::rename('queue__failed_jobs', 'failed_jobs');
        }
        
        if (Schema::hasTable('queue__job_batches')) {
            Schema::rename('queue__job_batches', 'job_batches');
        }
        
        if (Schema::hasTable('queue__jobs')) {
            Schema::rename('queue__jobs', 'jobs');
        }
    }
};