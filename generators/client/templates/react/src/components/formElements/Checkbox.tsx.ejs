import { useFormContext, RegisterOptions, FieldValues } from 'react-hook-form';
import { FormLabel } from './FormLabel';
import { FormErrors } from './FormErrors';

interface CheckboxProps {
  fieldName: string;
  label: string;
  validations?: {
    required?: boolean;
  };
}

const Checkbox: React.FC<CheckboxProps> = ({ fieldName, label, validations }) => {
  const { required } = validations || {};
  const {
    register,
    formState: { errors },
  } = useFormContext();

  const opts: RegisterOptions<FieldValues, string> | undefined = {};

  if (required) {
    opts.required = 'errors.validations.required';
  }

  return (
    <>
      <FormLabel htmlFor={fieldName} label={label} validations={validations} />
      <input
        type="checkbox"
        id={fieldName}
        {...register(fieldName, opts)}
        className={
          errors?.[fieldName]?.message
            ? 'form-field-error mx-auto w-5 h-5 p-3'
            : 'form-field mx-auto w-5 h-5 p-3'
        }
      />
      <FormErrors errors={errors} fieldName={fieldName} />
    </>
  );
};

export { Checkbox };
