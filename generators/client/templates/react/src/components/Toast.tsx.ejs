import { X, ChevronDown, ChevronUp } from 'lucide-react';
import { INotification, useNotifications } from '../contexts/NotificationContext';
import { useEffect, useState, useCallback, useMemo } from 'react';
import { useTranslation } from 'react-i18next';

export function Toast({ notification }: { notification: INotification }) {
  const { t } = useTranslation();
  const {
    removeNotification,
    pauseNotification,
    resumeNotification,
    getRemainingTime,
    hasAutoClose,
  } = useNotifications();

  const duration = notification.duration || 5000;
  const [remainingTime, setRemainingTime] = useState(duration);
  const [isPaused, setIsPaused] = useState(false);
  const [showTechDetails, setShowTechDetails] = useState(false);
  const hasTimer = hasAutoClose(notification.id);

  const colors = useMemo(
    () =>
      ({
        success: { bg: 'bg-green-500', progress: 'bg-green-400' },
        error: { bg: 'bg-red-500', progress: 'bg-red-400' },
        warning: { bg: 'bg-yellow-500', progress: 'bg-yellow-400' },
        info: { bg: 'bg-blue-500', progress: 'bg-blue-400' },
      })[notification.type],
    [notification.type]
  );

  useEffect(() => {
    if (!hasTimer || isPaused) return;

    const timer = setInterval(() => {
      const remaining = getRemainingTime(notification.id);
      setRemainingTime(remaining);
    }, 100);

    return () => clearInterval(timer);
  }, [isPaused, notification.id, getRemainingTime, hasTimer]);

  const handleMouseEnter = useCallback(() => {
    if (!hasTimer) return;
    setIsPaused(true);
    pauseNotification(notification.id);
  }, [notification.id, pauseNotification, hasTimer]);

  const handleMouseLeave = useCallback(() => {
    if (!hasTimer) return;
    setIsPaused(false);
    resumeNotification(notification.id);
  }, [notification.id, resumeNotification, hasTimer]);

  const progressStyle = useMemo(
    () => ({
      width: hasTimer ? `${(remainingTime / duration) * 100}%` : '100%',
      transition: isPaused ? 'none' : 'width linear 100ms',
    }),
    [remainingTime, duration, isPaused, hasTimer]
  );

  return (
    <div
      className={`${colors.bg} text-white rounded-lg shadow-lg min-w-[300px] max-w-[50vw] overflow-hidden`}
      onMouseEnter={handleMouseEnter}
      onMouseLeave={handleMouseLeave}
    >
      <div className="p-4 flex justify-between items-start">
        <div className="flex-1">
          <div className="flex items-center gap-2">
            <div>{notification.message}</div>
          </div>
          {notification.details && (
            <div className="mt-2 text-sm opacity-75 bg-black/10 p-2 rounded">
              <div>
                {notification.details}
                {notification.techDetails && !showTechDetails && (
                  <span className="text-xs">
                    {' '}
                    (
                    <a
                      href="#"
                      onClick={() => setShowTechDetails(!showTechDetails)}
                      className="hover:text-gray-200"
                    >
                      {t('common.show.more')} ...
                    </a>
                    )
                  </span>
                )}
              </div>
              {showTechDetails && notification.techDetails && (
                <>
                  <pre className="mt-2 text-xs text-wrap text-white/80">
                    {notification.techDetails}
                  </pre>
                  {notification.techDetails && showTechDetails && (
                    <span className="text-xs">
                      {' '}
                      (
                      <a
                        href="#"
                        onClick={() => setShowTechDetails(!showTechDetails)}
                        className="hover:text-gray-200"
                      >
                        {t('common.show.less')}
                      </a>
                      )
                    </span>
                  )}
                </>
              )}
            </div>
          )}
        </div>
        <div className="flex items-center ml-2 gap-2 shrink-0">
          &nbsp;
          {hasTimer && (
            <span className="text-sm opacity-75">({Math.ceil(remainingTime / 1000)}s)</span>
          )}
          <button
            onClick={() => removeNotification(notification.id)}
            className="text-white hover:text-gray-200"
          >
            <X size={18} />
          </button>
        </div>
      </div>
      <div className="h-1 w-full">
        <div className={`h-full ${colors.progress}`} style={progressStyle} />
      </div>
    </div>
  );
}
