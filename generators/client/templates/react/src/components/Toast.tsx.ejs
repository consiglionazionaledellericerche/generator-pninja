// src/components/Toast.tsx
import { X } from 'lucide-react';
import { INotification, useNotifications } from '../contexts/NotificationContext';
import { useEffect, useState, useCallback } from 'react';

export function Toast({ notification }: { notification: INotification }) {
    const { removeNotification, pauseNotification, resumeNotification, getRemainingTime } = useNotifications();
    const duration = notification.duration || 5000;
    const [remainingTime, setRemainingTime] = useState(duration);
    const [isPaused, setIsPaused] = useState(false);

    const bgColorClass = {
        success: 'bg-green-500',
        error: 'bg-red-500',
        warning: 'bg-yellow-500',
        info: 'bg-blue-500'
    }[notification.type];

    const progressBgColorClass = {
        success: 'bg-green-400',
        error: 'bg-red-400',
        warning: 'bg-yellow-400',
        info: 'bg-blue-400'
    }[notification.type];

    useEffect(() => {
        if (isPaused) return;

        const timer = setInterval(() => {
            const remaining = getRemainingTime(notification.id);
            setRemainingTime(remaining);
        }, 10);

        return () => clearInterval(timer);
    }, [isPaused, notification.id, getRemainingTime]);

    const handleMouseEnter = useCallback(() => {
        setIsPaused(true);
        pauseNotification(notification.id);
    }, [notification.id, pauseNotification]);

    const handleMouseLeave = useCallback(() => {
        setIsPaused(false);
        resumeNotification(notification.id);
    }, [notification.id, resumeNotification]);

    const progressPercentage = (remainingTime / duration) * 100;
    const secondsRemaining = Math.ceil(remainingTime / 1000);

    return (
        <div
            className={`${bgColorClass} text-white rounded-lg shadow-lg min-w-[300px] overflow-hidden`}
            onMouseEnter={handleMouseEnter}
            onMouseLeave={handleMouseLeave}
        >
            <div className="p-4 flex justify-between items-center">
                <span>{notification.message}</span>
                <div className="flex items-center ml-2 gap-2">
                    <span className="text-sm opacity-75">({secondsRemaining}s)</span>
                    <button
                        onClick={() => removeNotification(notification.id)}
                        className="text-white hover:text-gray-200"
                    >
                        <X size={18} />
                    </button>
                </div>
            </div>
            
            <div className="h-1 w-full">
                <div 
                    className={`h-full ${progressBgColorClass} transition-all duration-100 ease-linear`}
                    style={{ width: `${progressPercentage}%` }}
                />
            </div>
        </div>
    );
}