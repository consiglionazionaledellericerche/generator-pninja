import { LoaderCircle as Loader } from 'lucide-react';
export function SimpleLoader({ loading = true, size = 48 }) {
  return loading ? <Loader size={size} className={`animate-spin text-blue-600 dark:text-blue-500`} /> : null;
}
