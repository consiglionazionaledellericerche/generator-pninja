import { useEffect } from 'react';
import { useTranslation } from 'react-i18next';
export default function Err401() {
  const { t, i18n } = useTranslation();
  useEffect(() => {
    document.title = t('errors.401.title');
  }, [t, i18n.language]);
  return (
    <div className="m-4">
      <h1 className="text-2xl mb-4">{t('errors.401.title')}</h1>
      <p>{t('errors.401.message')}</p>
    </div>
  );
}
