import prestoLogo from '../assets/disegno.svg';
import laravelLogo from '../assets/laravel-logo.svg';
import <%= dbms %>Logo from '../assets/<%= dbms %>-logo.svg';
import reactLogo from '../assets/react.svg';
import viteLogo from '../assets/vite.svg';

export default function Home() {
  return (
    <div className="flex space-x-2 items-center text-3xl justify-center p-8">
      <img className="h-16" src={laravelLogo} alt="Laravel" title="Laravel" />
      <span>+</span>
      <img className="h-16" src={<%= dbms %>Logo} alt="<%= dbms %>" title="<%= dbms %>" />
      <span>+</span>
      <img className="h-16" src={reactLogo} alt="React" title="React" />
      <span>+</span>
      <img className="h-16" src={viteLogo} alt="Vite" title="Vite" />
      <span>=</span>
      <img className="h-16" src={prestoLogo} alt="Presto" title="Presto" />
    </div>
  );
}
